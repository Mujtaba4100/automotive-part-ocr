# OEM Part Number Detector — Phase 1 MVP
# Python 3.10+ required

# ── GUI ──────────────────────────────────────────────────────────────
PySide6>=6.6.0,<6.12

# ── Image processing ─────────────────────────────────────────────────
opencv-python>=4.9.0
Pillow>=10.3.0

# ── OCR ──────────────────────────────────────────────────────────────
# Pinned to 2.x: PaddlePaddle 3.x has an unresolved OneDNN/PIR
# incompatibility on Windows (ConvertPirAttribute2RuntimeAttribute).
# PaddleOCR 2.7.3 + PaddlePaddle 2.6.2 are the stable Windows builds.
paddleocr==2.7.3
paddlepaddle==2.6.2        # CPU; replace with paddlepaddle-gpu for GPU

# ── Utilities ────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0         # PaddlePaddle 2.x requires numpy < 2.0
protobuf>=3.1.0,<=3.20.2   # PaddlePaddle 2.6.2 requires protobuf <= 3.20.2 on Windows
